// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://brentellingson.github.io',
    base: '/astro-tutorial-2025',
});
